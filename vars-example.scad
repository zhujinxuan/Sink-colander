// Vars for connector
wallThick = 4;
connectY = wallThick + 1;

// Basic variables for calculating the hanger
faucetD = 40;
hangerTopPadding = 8;
hangerBottomPadding = 16+7+2;
hangerBaseHeight = 8;

entreConnectors = faucetD + hangerTopPadding;

// Basic vars for colander
colanderWidth = 150;  // X
colanderLength = 80; // Y
colanderDepth = 90;

